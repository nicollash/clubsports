import { StyleSheet } from '@react-pdf/renderer';

const styles = StyleSheet.create({
  thead: {
    flexDirection: 'row',
    margin: '0 10px 0 67px',
    borderTop: 1,
    borderBottom: 1,
    borderColor: '#333',
  },
  fieldName: {
    width: 95.5,

    textAlign: 'center',

    padding: '5px',

    fontSize: 9,
  },
});

export { styles };
