enum ApplicationTypes {
  STAGING = 'staging',
  MEMBERS = 'members',
}

// TP - Pool play
enum MobileEventStatuses {
  EVENT = '1',
  EVENT_TP = '2',
  EVENT_TP_BRACKETS = '3',
}

export { ApplicationTypes, MobileEventStatuses };
